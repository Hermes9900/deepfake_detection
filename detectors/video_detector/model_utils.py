import numpy as np

def predict_frame(frame):
    """
    Placeholder: compute manipulation score per frame
    Replace with call to image_detector.predict_image(frame)
    """
    return np.random.uniform(0,1)

def predict_av_sync(video_path):
    """
    Placeholder AV-sync detection
    Returns 0-1 score (0=fake, 1=perfect sync)
    """
    return np.random.uniform(0,1)
